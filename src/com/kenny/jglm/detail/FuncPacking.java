package com.kenny.jglm.detail;

/**
 * Implementations from <b>func_packing.inl </b> API.
 * 
 * @author Kenny
 */
public class FuncPacking {
	
	/*
	
	public static int packUnorm2x16(Vector2f v) {
		
		Vector2s result = new Vector2s(Common.round(Operator.mul(Common.clamp(v, 0.0f, 1.0f), 65535.0f)));

		u.in[0] = result[0];
		u.in[1] = result[1];

		return u.out;
	}
	 * 
	 */
}
